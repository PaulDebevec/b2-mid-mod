<h1><%= @movie.title %></h1>

<section id="actors">
  <% @movie.actors_by_age.each do |actor| %>
    <%= actor.name %>
  <% end %>
</section>

<%= form_tag "/movies/#{@movie.id}", method: :patch do %>
  <%= label_tag :name %>
  <%= text_field_tag :name %>
  <%= submit_tag "Add Actor" %>
<% end %>
